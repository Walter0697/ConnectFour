using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MiniMaxScript : MonoBehaviour
{
    private struct Node
    {
        public int index;
        public int value;
    };

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public static int numOfConnectFour(int[,] gameGrid, int key)
    {
        return 0;
    }

    public static int numOfConnectThree(int[,] gameGrid, int key)
    {
        return 0;
    }

    public static int numOfConnectTwo(int[,] gameGrid, int key)
    {
        return 0;
    }

    public static int minimax(int[,] gameGrid, int depth, bool maximizingPlayer, int playerKey)
    {
        return 0;
    }
}
